from setuptools import setup, find_packages


setup(
    name='compacthdwallets',
    version='0.0.1dev',
    description='',
    python_requires='>=3.6.0',
    packages=find_packages(exclude=['tests']),
)
